This is a parking app which helps users find open parking spots in the city of chicago. It is built with javascript, html/css, and node.js.

Changed database containing parking house data from public to local database. Currently, this is as far as a request to mongodb to display data in json format with the url: '/parkinghouses', simply to ensure connection.